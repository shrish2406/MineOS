import { useLocation } from 'react-router-dom'
import { EmptyState, SectionCard } from '../components/ui'

const purpose: Record<string, string> = { compliance: 'Track statutory obligations, document evidence and compliance status.', inspections: 'Plan, record and follow up on field and statutory inspections.', violations: 'Review violations, severity, ownership and closure status.', actions: 'Coordinate corrective actions and due-date escalation.', reports: 'Prepare governance, operational and regulatory reporting.', alerts: 'Review operational alerts requiring management visibility.', contractors: 'Manage contractor governance and safety compliance records.', safety: 'Capture and review safety observations from field activity.', incidents: 'Record incidents and connect their corrective actions.', performance: 'Compare operational performance across mines.', risk: 'Monitor enterprise compliance and operational risk signals.', monitoring: 'Monitor mines within the regulatory oversight scope.', records: 'Review inspection records and regulatory submissions.', settings: 'Manage workspace preferences and future integration settings.' }
export function ModulePage() { const key = useLocation().pathname.split('/').pop() ?? ''; const title = key.split('-').map((word) => word[0]?.toUpperCase() + word.slice(1)).join(' '); return <div className="max-w-3xl"><p className="text-sm font-semibold text-minsos-600">Day 1 foundation</p><h2 className="mt-1 text-2xl font-bold text-slate-900">{title}</h2><p className="mt-2 text-slate-600">{purpose[key] ?? 'This governance module is part of the MINSOS information architecture.'}</p><SectionCard className="mt-6" title="Module workspace" subtitle="Planned for the next delivery phases"><EmptyState title="Module ready for backend integration" detail="The route, navigation and consistent workspace shell are established. Live data and workflows will be added after the corresponding API contract is agreed." /></SectionCard></div> }
