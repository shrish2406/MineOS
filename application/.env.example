# Replace with the LAN address of the computer running the MineOS backend.
# For Android/iOS physical devices, both devices must be on the same Wi-Fi network.
EXPO_PUBLIC_API_URL=http://10.106.149.199:5000/api
